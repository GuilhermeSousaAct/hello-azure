# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - saturn-prosperi(hello-azure-app)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://saturnoprosperi.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_48a105237d364fa9b5b430e52ddbe3b5 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_9dcddbfd5a8f477e813155af6fccfc51 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: saturnoprosperi.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_48a105237d364fa9b5b430e52ddbe3b5 }}/node:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'hello-azure-app'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'saturn-prosperi'
        slot-name: 'hello-azure-app'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_f842628c930849dfa5551686e7896d0b }}
        images: 'saturnoprosperi.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_48a105237d364fa9b5b430e52ddbe3b5 }}/node:${{ github.sha }}'